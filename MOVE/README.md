# Clean MOVE

This package contains the streamlined implementation of the MOVE algorithm.
It exposes the `MOVE` class and a CLI-compatible `main` entry point. You can
run the algorithm directly from this directory by executing:

```bash
python -m clean
```
